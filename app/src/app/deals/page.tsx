import { CityListingChannelPage } from "@/components/listings/ListingKit";

// Cross-city (nationwide) merchant-deals index — no city pre-selected.
export default function DealsIndexPage() {
  return <CityListingChannelPage kind="deals" />;
}
