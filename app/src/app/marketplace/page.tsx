import { CityListingChannelPage } from "@/components/listings/ListingKit";

// Cross-city (nationwide) second-hand marketplace index — no city pre-selected.
export default function MarketplaceIndexPage() {
  return <CityListingChannelPage kind="marketplace" />;
}
