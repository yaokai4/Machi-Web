import { CityListingChannelPage } from "@/components/listings/ListingKit";

// Cross-city (nationwide) jobs index — no city pre-selected.
export default function JobsIndexPage() {
  return <CityListingChannelPage kind="jobs" />;
}
